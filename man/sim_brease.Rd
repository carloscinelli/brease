% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prior.R
\name{sim_brease}
\alias{sim_brease}
\title{Simulate from the BREASE Prior}
\usage{
sim_brease(
  n = 10000,
  mu0 = 0.5,
  n0 = 2,
  mue = 0.5,
  ne = mu0 * n0,
  mus = 0.5,
  ns = (1 - mu0) * n0
)
}
\arguments{
\item{n}{Number of samples to draw.}

\item{mu0}{Prior mean for baseline risk (default 0.5).}

\item{n0}{Prior sample size for baseline risk (default 2).}

\item{mue}{Prior mean for efficacy (default 0.5).}

\item{ne}{Prior sample size for efficacy (default \code{mu0 * n0}).}

\item{mus}{Prior mean for side-effect risk (default 0.5).}

\item{ns}{Prior sample size for side-effect risk (default \code{(1 - mu0) * n0}).}
}
\value{
A data.frame with columns: \code{theta0}, \code{eta_e},
  \code{eta_s}, \code{theta1}.
}
\description{
Generates prior predictive samples from the BREASE model.
}
\examples{
# Default prior
prior_samps <- sim_brease(10000)
plot(prior_samps$theta0, prior_samps$theta1,
     xlab = expression(theta[0]), ylab = expression(theta[1]),
     pch = ".", col = "gray")

# Informative prior centered at low risk and moderate efficacy
prior_samps <- sim_brease(10000, mu0 = 0.01, n0 = 10,
                           mue = 0.5, ne = 5, mus = 0.01, ns = 5)

}
\references{
Irons, N.J. and Cinelli, C. (2025). Causally Sound Priors for Binary
Experiments. \emph{Bayesian Analysis}. \doi{10.1214/25-BA1506}
}
