% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prior.R
\name{sim_lt}
\alias{sim_lt}
\title{Simulate from the Logit-Transform Prior}
\usage{
sim_lt(n = 10000, mu_psi = 0, var_psi = 1, mu_beta = 0, var_beta = 1)
}
\arguments{
\item{n}{Number of samples to draw.}

\item{mu_psi}{Prior mean for log-odds ratio (default 0).}

\item{var_psi}{Prior variance for log-odds ratio (default 1).}

\item{mu_beta}{Prior mean for log-odds baseline (default 0).}

\item{var_beta}{Prior variance for log-odds baseline (default 1).}
}
\value{
A data.frame with columns: \code{psi}, \code{beta},
  \code{theta0}, \code{theta1}.
}
\description{
Generates prior predictive samples from the logit-transform
  (LT) prior, which places normal priors on the log-odds baseline
  (\eqn{\beta}) and log-odds ratio (\eqn{\psi}).
}
\examples{
prior_samps <- sim_lt(10000)
plot(prior_samps$theta0, prior_samps$theta1, pch = ".", col = "gray")

}
