% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior_stan.R
\name{stan_brease}
\alias{stan_brease}
\title{Stan MCMC Sampling for the BREASE Model}
\usage{
stan_brease(
  n = 10000,
  y0,
  y1,
  N0,
  N1,
  mu0 = 0.5,
  n0 = 2,
  mue = 0.5,
  ne = 2,
  mus = 0.5,
  ns = 2,
  iter = 10000,
  chains = 4,
  warmup = iter\%/\%2,
  mono = FALSE,
  ...
)
}
\arguments{
\item{n}{Number of posterior samples to draw.}

\item{y0}{Number of events in the control group.}

\item{y1}{Number of events in the treatment group.}

\item{N0}{Number of subjects in the control group.}

\item{N1}{Number of subjects in the treatment group.}

\item{mu0}{Prior mean for baseline risk (default 0.5).}

\item{n0}{Prior sample size for baseline risk (default 2).}

\item{mue}{Prior mean for efficacy (default 0.5).}

\item{ne}{Prior sample size for efficacy (default 2).}

\item{mus}{Prior mean for side-effect risk (default 0.5).}

\item{ns}{Prior sample size for side-effect risk (default 2).}

\item{iter}{Total number of iterations per chain (default 10000).}

\item{chains}{Number of MCMC chains (default 4).}

\item{warmup}{Number of warmup iterations per chain (default \code{iter / 2}).}

\item{mono}{Logical; if \code{TRUE}, use the monotone (no-harm) model
(default \code{FALSE}).}

\item{...}{Additional arguments passed to \code{rstan::sampling()}.}
}
\value{
A list with components:
\describe{
  \item{theta0}{Posterior samples of baseline risk.}
  \item{theta1}{Posterior samples of treatment group risk.}
  \item{eta_e}{Posterior samples of efficacy.}
  \item{eta_s}{Posterior samples of side-effect risk (0 if monotone).}
  \item{stanfit}{The raw stanfit object for diagnostics.}
}
}
\description{
Uses Stan (via the \pkg{rstan} package) to draw posterior
  samples from the BREASE model. Requires \pkg{rstan} to be installed.
}
\references{
Irons, N.J. and Cinelli, C. (2025). Causally Sound Priors for Binary
Experiments. \emph{Bayesian Analysis}. \doi{10.1214/25-BA1506}
}
